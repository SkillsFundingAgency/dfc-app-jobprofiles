@model IndexViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <title>Job profiles Summary List</title>
</head>
<body>
    @if (Model.Documents?.Count() > 0)
    {
        <h2>Job Profiles Summary List</h2>
        <ul>
            @foreach (var document in Model.Documents)
            {
                <li>
                    <a href="/profile/@document.CanonicalName">@document.CanonicalName</a>
                </li>
            }
        </ul>
    }
</body>
</html>